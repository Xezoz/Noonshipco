const baseURL = import.meta.env.VITE_API_BASE_URL || "http://localhost:5080"; // The backend source API

const servicePrice = 15; //The price of every package will be processed

export default { baseURL, servicePrice };
